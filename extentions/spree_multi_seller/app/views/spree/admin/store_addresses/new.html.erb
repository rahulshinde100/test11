<%= render :partial => 'spree/admin/shared/seller_sub_menu', :locals => { :seller => @seller, :title => "Store Addresses" } %>
<%= render :partial => 'spree/shared/error_messages', :locals => { :target => @store_address } %>

<%= form_for [:admin, @seller, @store_address] do |f| %>
  <fieldset data-hook="new_seller">
    <legend align="center">Store Address</legend>
      <%= render :partial => "form", :locals => {:f => f} %>
      <div class="form-buttons filter-actions actions" data-hook="buttons">
        <%= button "Save & Continue", 'icon-ok' %>
        <span class="or"><%= Spree.t(:or) %></span>
        <%= link_to_with_icon 'icon-remove', Spree.t('actions.cancel'), admin_seller_store_addresses_path(@seller), :class => 'button' %>
      </div>
  </fieldset>
<%end%>